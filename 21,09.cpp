#include <iostream>
#include <cmath>
using namespace std;

/*int main() {
	double x, e , sum, a, k;
	int n = 4;
	cin >> x;
	cin >> e;
	sum = -x / 6;
		a = sum;
	while (fabs(a) > e) { 
		k = ((-1) * x * x) / (n * (n + 1)); 
		a = a * k;
		sum += a;
		n += 2;
	}
	cout << sum;

}
*/
/*
int main() {
	int x;
	cin >> x;
	const int  n = 10;
	int A[n];
	for (int i = 0; i < (n + 1); i++) {
		cin >> A[i];
		int sum = 0;
	for (int i = n; i != -1, i--;) {
		sum = sum - x + A[i];

		}
	cout << sum;
	}
}
*/
